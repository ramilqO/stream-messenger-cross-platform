export type ThemeConfig = {
    isDark: boolean
    colors: {
        primary: string
        background: string
        surface: string
        textPrimary: string
        bubbleMe: string
        bubbleThem: string
    }
    metrics: {
        borderRadius: number
        fontSize: number
    }
}
